en:
  activerecord:
    attributes:
      user:
        email: "Email"
    errors:
      models:
        user:
          attributes:
            scorer_id:
              existence: is not valid
        case:
          attributes:
            scorer_id:
              existence: is not valid
        scorer:
          attributes:
            scale:
              length: must be limited to at most 10 values
              type: is invalid (only integers allowed)
              existence: is not valid
              
