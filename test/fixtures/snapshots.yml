# == Schema Information
#
# Table name: snapshots
#
#  id         :integer          not null, primary key
#  name       :string(250)
#  created_at :datetime
#  case_id    :integer
#  updated_at :datetime         not null
#

empty_snapshot:
  case: :snapshot_case
  name: Empty Snapshot

a_snapshot:
  case: :snapshot_case
  name: A Snapshot
