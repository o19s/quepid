# Data Mapping

## Users/Teams

The `User` model is a typical model used for authenticating and authorizing a user in the app.

Everything starts with a user. The user owns entities they create, so if a user is removed all associated objects are removed as well.

In order to share stuff (eg. cases, scorers, etc) with other users, a user can create a `Team` and add any existing user to the team. Anything shared with a team is then shared with any members of the team.
The user that creates a `Team` is both the owner of the team and a member in the team.

## Live interactions with Search Engine

Everything related with search starts with the `Case` model.
A case is an entity that encompasses everything that has to do with an experiment or a test or work to be done in relations to a search setup.

The `Case` itself is a connector of all of the different elements that go with it and serves as the central place to get to other entities. The main attribute of a case is its `name`.

A case has many queries. A `Query` is a representation of a search term and all of its relevant info.
A query starts with the `query_text`, but can also have `notes` or `options` associated with it. Queries can also be re-ordered within a case.
A query returns results from the search engine, but those results are not saved or modeled in Quepid, Quepid does not keep track of search results. However, each results can be assigned a `Rating`.

A rating is associated to the query using the `query_id` foreign key and to a search results through the `doc_id` attribute. The `doc_id` is the only thing Quepid saves related to search results.

Ratings for each result are summed up and turned into query score using a `Scorer`. Each query can either have a specific unit test style scorer, or use the case scorer. Scorers can be created by a user to be used on cases and shared with teams, or be created in an ad-hoc manner directly for a query as a unit test.  There is an argument for unit test style scorers should be their own model and not shared with case level scorers.

The score of each query is transformed into a percentile score for the case, and saved as a time series as the `Score` model. The user can also create an `Annotation` which would be associated to a score, in order to save notes throughout time to indicate what changes were made that resulted in a different case score.

Each case has its own settings, and those settings or configs are saved in the `Try` model. Each time the configs are changed, a new `Try` record is created in the db to keep a history of the changes throughout time. Tries represent tweaks where developers fiddle with the search engine configs and test the results. A history is kept in order to make it easy to go back to a point in time and start over.

A `Try` connects to the individual Search engine via a `SearchEndpoint`.   The `SearchEndpoint` has a variety of properties that configure access. We have some like Solr or OpenSearch.  However, the SearchAPI variation of a `SearchEndpoint` lets you provide some custom JavaScript to map from any search response format to the specific one that Quepid expects.

The last remaining piece of the puzzle is `Snapshot`/`SnapshotQuery`/`SnapshotDoc`. A snapshot represents a snapshot of a point in time in history of queries and search results. The `Snapshot` model itself has a `name`, a reference to the case, and a time stamp. `SnapshotQuery` is a join model between a `Query` and a `Snapshot`. And a `SnapshotDoc` represents the `doc_id` of the result as well as the position they appear at in the search results at the time the snapshot was taken.

## Collecting Feedback on Search Quality

To support collecting search feedback from multiple Users, we introduced a similar data structure to Cases/Queries/Ratings that starts with a `Book`.   A `Book` represents a set of queries and their results, modeled as `QueryDocPair`'s.   Each `QueryDocPair` can have multiple `Judgements`, each made by a unique `User`.   

Unlike a `Case` datamodel that is meant for live interaction with a SearchEndpoint, the Book is meant to support a offline interaction model for gathering Judgements.

The data modeled by a Book can be imported back into a Case.  We take all the Judgements for a QueryDocPair, and average them, and then use that to populate the Rating for the corresponding Query in the Case.

## Other

Quepid provides scorers that are written by the OSC team for everyone to use, those `Scorer`'s are tagged with the `communal` flag as `true`, and the default scorers are created when seeding the db (using `bin/rake db:seed` or `bin/rake db:setup`).

## Entity Resolution Diagram

<!-- BEGIN ERD: generated by `rake erd:image`, do not edit by hand -->
```mermaid
erDiagram
	direction LR
	"ActiveStorage::Attachment" {
		integer blob_id FK
		string name
		integer record_id FK
		string record_type
	}
	"ActiveStorage::Blob" {
		integer byte_size
		string checksum
		string content_type
		string filename
		string key
		text metadata
		string service_name
	}
	"ActiveStorage::VariantRecord" {
		integer blob_id FK
		string variation_digest
	}
	"ActiveStorageDB::File" {
		binary data
		string ref
	}
	"Ahoy::Event" {
		string name
		json properties
		datetime time
		integer user_id FK
		integer visit_id FK
	}
	"Ahoy::Visit" {
		string app_version
		string browser
		string city
		string country
		string device_type
		string ip
		text landing_page
		float latitude
		float longitude
		string os
		string os_version
		string platform
		text referrer
		string referring_domain
		string region
		datetime started_at
		text user_agent
		integer user_id FK
		string utm_campaign
		string utm_content
		string utm_medium
		string utm_source
		string utm_term
		string visit_token
		string visitor_token
	}
	Annotation {
		text message
		string source
		integer user_id FK
	}
	Announcement {
		integer author_id FK
		boolean live
		text text
	}
	AnnouncementViewed {
		integer announcement_id FK
		integer user_id FK
	}
	ApiKey {
		string token_digest
		integer user_id FK
	}
	"Blazer::Audit" {
		string data_source
		integer query_id FK
		text statement
		integer user_id FK
	}
	"Blazer::Check" {
		string check_type
		integer creator_id FK
		text emails
		datetime last_run_at
		text message
		integer query_id FK
		string schedule
		text slack_channels
		string state
	}
	"Blazer::Dashboard" {
		integer creator_id FK
		string name
	}
	"Blazer::DashboardQuery" {
		integer dashboard_id FK
		integer position
		integer query_id FK
	}
	"Blazer::Query" {
		integer creator_id FK
		string data_source
		text description
		string name
		text statement
		string status
	}
	Book {
		boolean archived
		string export_job
		string import_job
		string name
		integer owner_id FK
		string populate_job
		string scale
		text scale_with_labels
		text scoring_guidelines
		boolean show_rank
		boolean support_implicit_judgements
	}
	BookMetadatum {
		integer book_id FK
		datetime last_viewed_at
		integer user_id FK
	}
	Case {
		boolean archived
		boolean auto_populate_book_pairs
		boolean auto_populate_case_judgements
		integer book_id FK
		string case_name
		integer last_try_number
		boolean nightly
		json options
		integer owner_id FK
		boolean public
		integer scorer_id FK
	}
	CaseMetadatum {
		integer case_id FK
		datetime last_viewed_at
		integer user_id FK
	}
	CuratorVariable {
		string name
		integer try_id FK
		float value
	}
	Judgement {
		text explanation
		boolean judge_later
		integer query_doc_pair_id FK
		float rating
		boolean unrateable
		integer user_id FK
	}
	MapperWizardState {
		string basic_auth_credential
		text custom_headers
		text docs_mapper
		text html_content
		string http_method
		text number_of_results_mapper
		string search_url
		text test_query
		integer user_id FK
	}
	Query {
		integer arranged_at
		integer arranged_next
		integer case_id FK
		string information_need
		text notes
		json options
		string query_text
	}
	QueryDocPair {
		integer book_id FK
		string doc_id
		text document_fields
		string information_need
		text notes
		json options
		integer position
		string query_text
	}
	Rating {
		string doc_id
		integer query_id FK
		float rating
		integer user_id FK
	}
	Score {
		boolean all_rated
		integer annotation_id FK
		integer case_id FK
		binary queries
		float score
		integer scorer_id FK
		integer try_id FK
		integer user_id FK
	}
	Scorer {
		text code
		boolean communal
		string name
		integer owner_id FK
		string scale
		text scale_with_labels
		boolean show_scale_labels
	}
	SearchEndpoint {
		string api_method
		boolean archived
		string basic_auth_credential
		string custom_headers
		string endpoint_url
		text mapper_code
		string name
		json options
		integer owner_id FK
		boolean proxy_requests
		integer requests_per_minute
		string search_engine
		text test_query
	}
	Snapshot {
		integer case_id FK
		string name
		integer scorer_id FK
		integer try_id FK
	}
	SnapshotDoc {
		string doc_id
		text explain
		text fields
		integer position
		boolean rated_only
		integer snapshot_query_id FK
	}
	SnapshotQuery {
		boolean all_rated
		integer number_of_results
		integer query_id FK
		integer response_status
		float score
		integer snapshot_id FK
	}
	"SolidCable::Message" {
		binary channel
		integer channel_hash
		binary payload
	}
	"SolidQueue::BlockedExecution" {
		string concurrency_key
		datetime expires_at
		integer job_id FK
		integer priority
		string queue_name
	}
	"SolidQueue::ClaimedExecution" {
		integer job_id FK
		integer process_id FK
	}
	"SolidQueue::FailedExecution" {
		text error
		integer job_id FK
	}
	"SolidQueue::Job" {
		string active_job_id
		text arguments
		string class_name
		string concurrency_key
		datetime finished_at
		integer priority
		string queue_name
		datetime scheduled_at
	}
	"SolidQueue::Pause" {
		string queue_name
	}
	"SolidQueue::Process" {
		string hostname
		string kind
		datetime last_heartbeat_at
		text metadata
		string name
		integer pid
		integer supervisor_id FK
	}
	"SolidQueue::ReadyExecution" {
		integer job_id FK
		integer priority
		string queue_name
	}
	"SolidQueue::RecurringExecution" {
		integer job_id FK
		datetime run_at
		string task_key FK
	}
	"SolidQueue::RecurringTask" {
		text arguments
		string class_name
		string command
		text description
		string key
		integer priority
		string queue_name
		string schedule
		boolean static
	}
	"SolidQueue::ScheduledExecution" {
		integer job_id FK
		integer priority
		string queue_name
		datetime scheduled_at
	}
	"SolidQueue::Semaphore" {
		datetime expires_at
		string key FK
		integer value
	}
	Team {
		string name
	}
	Try {
		string ancestry
		integer case_id FK
		boolean escape_query
		string field_spec
		string name
		integer number_of_rows
		string query_params
		integer search_endpoint_id FK
		integer try_number
	}
	User {
		boolean administrator
		boolean agreed
		datetime agreed_time
		string company
		boolean completed_case_wizard
		integer default_scorer_id FK
		string email
		boolean email_marketing
		datetime invitation_accepted_at
		datetime invitation_created_at
		integer invitation_limit
		datetime invitation_sent_at
		string invitation_token
		integer invitations_count
		integer invited_by_id FK
		string llm_key
		boolean locked
		datetime locked_at
		string name
		integer num_logins
		json options
		string password
		string profile_pic
		datetime reset_password_sent_at
		string reset_password_token
		string stored_raw_invitation_token
		string system_prompt
	}
	WebRequest {
		integer integer
		binary request
		binary response
		integer response_status
		integer snapshot_query_id FK
	}
	User o|--}o "Blazer::Query" : ""
	"Blazer::Query" ||--}o "Blazer::Check" : ""
	"Blazer::Query" ||--}o "Blazer::DashboardQuery" : ""
	"Blazer::Query" o|--}o "Blazer::Audit" : ""
	"Blazer::Dashboard" ||--}o "Blazer::DashboardQuery" : ""
	User o|--}o "Blazer::Dashboard" : ""
	User o|--}o "Blazer::Check" : ""
	User o|--}o "Blazer::Audit" : ""
	"SolidQueue::RecurringTask" o|--}o "SolidQueue::RecurringExecution" : ""
	"SolidQueue::Process" ||--}o "SolidQueue::ClaimedExecution" : ""
	"SolidQueue::Process" o|--}o "SolidQueue::Process" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::RecurringExecution" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::FailedExecution" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::ScheduledExecution" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::BlockedExecution" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::ReadyExecution" : ""
	"SolidQueue::Job" o|--|o "SolidQueue::ClaimedExecution" : ""
	"SolidQueue::BlockedExecution" o|--|o "SolidQueue::Semaphore" : ""
	"ActiveStorage::VariantRecord" ||--}o "ActiveStorage::Blob" : ""
	Record ||--|o "ActiveStorage::Attachment" : ""
	"ActiveStorage::Blob" ||--|o "ActiveStorage::Attachment" : ""
	"ActiveStorage::VariantRecord" ||--|o "ActiveStorage::Attachment" : ""
	Book ||--|o "ActiveStorage::Attachment" : ""
	Snapshot ||--|o "ActiveStorage::Attachment" : ""
	"ActiveStorage::Blob" ||--}o "ActiveStorage::Attachment" : ""
	"Ahoy::Visit" ||--}o "Ahoy::Event" : ""
	User o|--}o "Ahoy::Visit" : ""
	User o|--}o "Ahoy::Event" : ""
	SnapshotQuery o|--|o WebRequest : ""
	User ||--}o ApiKey : ""
	User o|--}o Scorer : ""
	User o|--}o Case : ""
	User o{--}o Book : ""
	Team o{--}o User : ""
	User o|--}o Score : ""
	User o|--}o Judgement : ""
	User ||--}o CaseMetadatum : ""
	User ||--}o BookMetadatum : ""
	User ||--}o Announcement : ""
	User o|--}o User : ""
	Case o|--}o Try : ""
	SearchEndpoint o|--}o Try : ""
	Try ||--}o CuratorVariable : ""
	Try o|--}o Snapshot : ""
	Case o{--}o Team : ""
	Scorer o{--}o Team : ""
	SearchEndpoint o{--}o Team : ""
	Book o{--}o Team : ""
	Snapshot o|--}o SnapshotQuery : ""
	Query o|--}o SnapshotQuery : ""
	SnapshotQuery o|--}o SnapshotDoc : ""
	Case o|--}o Snapshot : ""
	Scorer o|--}o Snapshot : ""
	Scorer o|--}o Score : ""
	Case ||--}o Score : ""
	Try ||--}o Score : ""
	Annotation o|--|o Score : ""
	Query ||--}o Rating : ""
	User o|--}o Rating : ""
	Book ||--}o QueryDocPair : ""
	QueryDocPair ||--}o Judgement : ""
	Case ||--}o Query : ""
	User ||--}o MapperWizardState : ""
	Case ||--}o CaseMetadatum : ""
	Scorer o|--}o Case : ""
	Book o|--}o Case : ""
	Book ||--}o BookMetadatum : ""
	User ||--}o AnnouncementViewed : ""
	Announcement ||--}o AnnouncementViewed : ""
	User ||--}o Annotation : ""
	User o|--}o SearchEndpoint : ""
```
<!-- END ERD -->
