// Minimal JS entry point for the AngularJS core layout that
// is loading Rails Stimulus based Javascript.
// Only loads Stimulus controllers — no Turbo, no Bootstrap JS, no Vega, etc.

//import PocModalController from "controllers/poc_modal_controller"
import "controllers"
