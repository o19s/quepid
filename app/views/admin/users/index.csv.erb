<%
headers = [:id, :name, :email, :email_marketing, :agreed_time]
%>
<%= ::CSV.generate_line(headers).html_safe %><%
@users.each do |user|
%><%= ::CSV.generate_line(headers.map{ |attr| user.send(attr) }).html_safe %><%
end %>
