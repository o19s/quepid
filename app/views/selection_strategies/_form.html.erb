<%= form_with(model: selection_strategy) do |form| %>
  <% if selection_strategy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(selection_strategy.errors.count, "error") %> prohibited this selection_strategy from being saved:</h2>

      <ul>
        <% selection_strategy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
